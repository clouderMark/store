import {Container, Typography} from '@mui/material';

const Delivery = () => (
  <Container maxWidth={false} sx={{mb: 15}}>
    <Typography component='h1' sx={{mb: 4, mt: 4, fontSize: '30px'}}>Доставка</Typography>
  </Container>
);

export default Delivery;
