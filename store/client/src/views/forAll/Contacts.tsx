import {Container, Typography} from '@mui/material';

const Contacts = () => (
  <Container maxWidth={false} sx={{mb: 15}}>
    <Typography component='h1' sx={{mb: 4, mt: 4, fontSize: '30px'}}>Контакты</Typography>
  </Container>
);

export default Contacts;
