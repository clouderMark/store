export enum EType {
  reset = 'reset',
  valid = 'valid',
  name = 'name',
  cardImage = 'cardImage',
  cardImageUrl = 'cardImageUrl',
  headerImageUrl = 'headerImageUrl',
  headerImage = 'headerImage',
  fetch = 'fetch',
}
