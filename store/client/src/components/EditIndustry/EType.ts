export enum EType {
  reset = 'reset',
  valid = 'valid',
  name = 'name',
  cardImage = 'cardImage',
  cardImageUrl = 'cardImageUrl',
  sliderImage = 'sliderImage',
  sliderImageUrl = 'sliderImageUrl',
}
