enum EInfo {
  reset = 'reset',
  infoImages = 'infoImages',
  infoParagraphs = 'infoParagraphs',
  infoTitle = 'infoTitle',
  imagesValid = 'imagesValid',
  fetch = 'fetch',
}

export default EInfo;
